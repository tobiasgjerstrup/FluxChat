meta {
  name: Register (Get JWT)
  type: http
  seq: 4
}

post {
  url: {{URL}}/api/auth/register
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "usernamee",
    "password": "password",
    "email": "usernamee"
  }
}

script:post-response {
  const token = res.body.token;
  bru.setEnvVar("JWT", token);
}

settings {
  encodeUrl: true
  timeout: 0
}
