meta {
  name: Delete Message
  type: graphql
  seq: 25
}

post {
  url: {{URL}}/graphql
  body: graphql
  auth: bearer
}

auth:bearer {
  token: {{JWT}}
}

body:graphql {
  mutation {
    deleteMessage(id: 1)
  }
}
